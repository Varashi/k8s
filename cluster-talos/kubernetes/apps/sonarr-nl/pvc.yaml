apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-nl-config
  namespace: sonarr-nl
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 20Gi
