apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ocis-config
  namespace: ocis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 10Gi
