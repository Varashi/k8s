apiVersion: v1
kind: ConfigMap
metadata:
  name: ocis-web
  namespace: ocis
data:
  web.yaml: |
    ---
    web:
      config:
        apps:
          - files
          - search
          - pdf-viewer
          - admin-settings
