---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-nl-db-credentials
  namespace: sonarr-nl
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secretsmanager
  target:
    name: sonarr-nl-db-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: sonarr_nl
        password: "{{ .dbPassword }}"
  data:
    - secretKey: dbPassword
      remoteRef:
        key: SECRET_SONARR_NL_POSTGRES_PASSWORD
