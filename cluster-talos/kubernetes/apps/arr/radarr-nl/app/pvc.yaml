apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-nl-config
  namespace: radarr-nl
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 20Gi
