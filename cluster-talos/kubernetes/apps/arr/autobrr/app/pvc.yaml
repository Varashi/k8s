apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: autobrr-config
  namespace: autobrr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 1Gi
