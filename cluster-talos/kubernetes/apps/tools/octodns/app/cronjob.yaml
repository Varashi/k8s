---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: octodns-sync
  namespace: octodns
spec:
  schedule: "*/15 * * * *"
  timeZone: Europe/Brussels
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: octodns
              # renovate: datasource=docker depName=ghcr.io/varashi/octodns versioning=docker
              image: ghcr.io/varashi/octodns:latest@sha256:54ab5222e1aecef64ecb8175331c9e4a6ad8d6b4a08d79eab723e29d72776073
              imagePullPolicy: IfNotPresent
              args:
                - octodns-sync-filtered
                - --config-file
                - /etc/octodns/config.yaml
                - --doit
                # --force bypasses raise_if_unsafe guards (RootNsChange, bulk
                # deletes). Safe for a one-way AD→NetBox mirror where AD is
                # authoritative and NetBox is rebuilt each run.
                - --force
              envFrom:
                - secretRef:
                    name: octodns-netbox
              volumeMounts:
                - name: config
                  mountPath: /etc/octodns
                  readOnly: true
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
                runAsNonRoot: true
                allowPrivilegeEscalation: false
                capabilities:
                  drop: [ALL]
                seccompProfile:
                  type: RuntimeDefault
              resources:
                requests: { cpu: 50m, memory: 64Mi }
                limits: { memory: 256Mi }
          volumes:
            - name: config
              configMap:
                name: octodns-config
