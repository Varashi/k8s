apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-pms-vsan
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 50Gi
