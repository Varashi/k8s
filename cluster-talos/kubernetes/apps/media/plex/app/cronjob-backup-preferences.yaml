apiVersion: batch/v1
kind: CronJob
metadata:
  name: plex-backup-preferences
  namespace: plex
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          nodeSelector:
            intel.feature.node.kubernetes.io/gpu: "true"
          tolerations:
            - key: intel.feature.node.kubernetes.io/gpu
              operator: Equal
              value: "true"
              effect: PreferNoSchedule
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchLabels:
                      app.kubernetes.io/name: plex
                      app.kubernetes.io/controller: plex
                  topologyKey: kubernetes.io/hostname
          containers:
            - name: backup
              image: busybox:latest
              command:
                - /bin/sh
                - -c
                - |
                  set -eu
                  DATE=$$(date +%Y-%m-%d)
                  mkdir -p /backup/prefs

                  # Back up Preferences.xml
                  cp "/config/Library/Application Support/Plex Media Server/Preferences.xml" \
                     "/backup/prefs/Preferences.xml-$${DATE}"
                  echo "Backed up Preferences.xml as prefs/Preferences.xml-$${DATE}"
                  ls -t /backup/prefs/Preferences.xml-* | tail -n +15 | xargs -r rm -v

                  # Back up agent preferences (Plug-in Support/Preferences/)
                  tar -czf "/backup/prefs/PluginPreferences-$${DATE}.tar.gz" \
                    -C "/config/Library/Application Support/Plex Media Server/Plug-in Support" \
                    Preferences
                  echo "Backed up Plug-in Support/Preferences as prefs/PluginPreferences-$${DATE}.tar.gz"
                  ls -t /backup/prefs/PluginPreferences-*.tar.gz | tail -n +15 | xargs -r rm -v
              volumeMounts:
                - name: config-pms
                  mountPath: /config
                - name: backup
                  mountPath: /backup
          volumes:
            - name: config-pms
              persistentVolumeClaim:
                claimName: plex-config-pms-vsan
            - name: backup
              nfs:
                server: ${SECRET_NFS_HOST}.${SECRET_DOMAIN}
                path: /mnt/DATA/backup/plex
