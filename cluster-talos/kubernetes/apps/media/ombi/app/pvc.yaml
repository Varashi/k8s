apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ombi-config
  namespace: ombi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 10Gi
