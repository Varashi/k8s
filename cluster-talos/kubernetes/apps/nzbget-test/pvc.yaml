apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbget-test-config
  namespace: nzbget-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vsan
  resources:
    requests:
      storage: 5Gi
