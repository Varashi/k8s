apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
  namespace: cilium
spec:
  secretName: wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.${SECRET_DOMAIN}"
